# Rename this file to .env and update the values before running the app
# cp .env.example .env

# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=medstock

# Note: if running with Docker Compose, set DB_HOST=postgres

# Server
PORT=5000
